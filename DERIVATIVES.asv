%% This script is for calculating the derivatives of the model equations
%% SECOND ORDER
syms x1 x2
F = @(x1,x2) getAerodynamics(x1,x2,v)/(J*x1) - Dg*x1/J + (Dg*omega_0)/(J*p);

X1 = diff(F(x1,x2),x1);
X2 = diff(F(x1,x2),x2);

% X1(x0(1),x0(2))
X1